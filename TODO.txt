scratch area + TODO keeper for sjasmplus developers:
====================================================


====================
Ped:

INCHOB "..",7  bugs out with "file too short"
INCBIN "..",7  seems to work, but length printed to output is fake "0"

defines divne (pozri ci sa to neda prepojit s enums co su pre Error()):
#define MAXPASSES 3
#define LASTPASS 3

build scripts - extend with argument reading, to run only tests in particular subdirectory

LUA quirks:
    -- v includelua:
    function inc_ld(reg, val)
        _pc('LD '..reg..','..val)
    end
    -- v kode:
    LUA
        inc_ld(A, pass)     -- bug, should be 'A'
    ENDLUA
= no output, no error, just dies. ... Seems like any LUA syntax error does that, just dies.

Preco ENDM nefunguje ako EDUP/ENDR??? (implementacia tam aspon z casti je)

Z80: continue to refactor the multi-argument while('o'), to introduce OFF option later

====================
Docs:
http://z00m128.github.io/sjasmplus/documentation.html#po_output  - kecy o output-e vzdy na disku
REPT + DUP nemaju popis ako sa ukoncuju (EDUP, ENDR)
pridat <anchor id="po_***" /> pre vsetky pseudo-ops, aby sa dalo linkovat na kazdu z nich

